/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Uncomment below to enable image optimization for external sources
  // images: {
  //   remotePatterns: [
  //     { protocol: 'https', hostname: '**' },
  //   ],
  // },
};

module.exports = nextConfig;
